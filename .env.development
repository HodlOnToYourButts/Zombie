# Zombie Development Configuration
# This file contains development-only settings and should NOT be used in production

# Development mode flag
NODE_ENV=development
DEVELOPMENT_MODE=true

# CouchDB Admin Credentials (for automatic database setup)
COUCHDB_USER=admin
COUCHDB_PASSWORD=admin

# Zombie Application User (admin of zombieauth database only)
COUCHDB_APP_USER=zombieauth
COUCHDB_APP_PASSWORD=zombieauth
COUCHDB_DATABASE=zombieauth
COUCHDB_URL=http://couchdb:5984

# Instance Configuration
INSTANCE_ID=development
INSTANCE_LOCATION=docker-compose

# OIDC Configuration
ISSUER=http://localhost:8080

# Zombie Security Configuration (DEVELOPMENT ONLY - CHANGE IN PRODUCTION)
ZOMBIEAUTH_JWT_SECRET=dev-jwt-secret-change-in-production
ZOMBIEAUTH_SESSION_SECRET=dev-session-secret-change-in-production
ZOMBIEAUTH_MODE=development

# Server Configuration
PORT=8080